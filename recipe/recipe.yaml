schema_version: 1

context:
  name: array-api-strict
  version: "2.5"
  python_min: '3.10'

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/array_api_strict-${{ version }}.tar.gz
  sha256: 7767e597fa70c04c5db640114774d1a481c5f0a1a451a804ea1ccdd2a809e1c5

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools <=75
    - setuptools_scm >8
  run:
    - python >=${{ python_min }}
    - numpy

tests:
  - python:
      imports:
        - array_api_strict
      pip_check: true

      python_version: ${{ python_min }}.*
about:
  summary: A strict, minimal implementation of the Python array API standard.
  license: BSD-3-Clause
  license_file: LICENSE
  homepage: https://data-apis.org/array-api-strict/

extra:
  recipe-maintainers:
    - lucascolley
    - asmeurer
    - rgommers
