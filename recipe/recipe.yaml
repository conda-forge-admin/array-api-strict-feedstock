schema_version: 1

context:
  name: array-api-strict
  version: "2.6.1"
  python_min: '3.10'

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/array_api_strict-${{ version }}.tar.gz
  sha256: 27485fd687c678894fb193e8f9f5116651a0bfc19dccea164d7d5df83b4e9a82

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools <=75
    - setuptools_scm >8
  run:
    - python >=${{ python_min }}
    - numpy

tests:
  - python:
      imports:
        - array_api_strict
      pip_check: true

      python_version: ${{ python_min }}.*
about:
  summary: A strict, minimal implementation of the Python array API standard.
  license: BSD-3-Clause
  license_file: LICENSE
  homepage: https://data-apis.org/array-api-strict/

extra:
  recipe-maintainers:
    - lucascolley
    - asmeurer
    - rgommers
